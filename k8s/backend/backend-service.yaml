apiVersion: v1
kind: Service
metadata:
  name: gestfarma-backend
spec:
  type: NodePort
  selector:
    app: gestfarma-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
