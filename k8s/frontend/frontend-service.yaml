apiVersion: v1
kind: Service
metadata:
  name: gestfarma-frontend
spec:
  type: NodePort
  selector:
    app: gestfarma-frontend
  ports:
    - protocol: TCP
      port: 5173
      targetPort: 5173
      nodePort: 30000
